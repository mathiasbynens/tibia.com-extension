{
	"private": true,
	"scripts": {
		"scrape": "node tools/scrape.js",
		"zip": "zip -r dist.zip css data img js rules LICENSE-MIT.txt manifest.json && unzip -l dist.zip"
	},
	"devDependencies": {
		"jsesc": "^2.5.2",
		"puppeteer": "^2.0.0"
	}
}
